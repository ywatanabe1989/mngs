# Time-stamp: "ywatanabe (2024-11-08 00:28:42)"
# File: ./mngs_repo/.github/workflows/install-pypi-latest.yml

name: install-pypi-latest

on:
  push:
    branches:
      - main      
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: install-pypi-latest
      run: |
          pip install -U pip        
          pip install mngs
          python ./tests/test_mngs_run.py
